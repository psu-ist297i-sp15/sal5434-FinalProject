<%= form_for(@shake) do |f| %>
  <% if @shake.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shake.errors.count, "error") %> prohibited this shake from being saved:</h2>

      <ul>
      <% @shake.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
